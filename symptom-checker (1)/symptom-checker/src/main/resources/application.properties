spring.application.name=symptom-checker

# MongoDB (use environment variable in Render)
spring.data.mongodb.uri=${MONGO_URI}

# Optional: server port (Render provides its own PORT environment variable)
server.port=${PORT:8080}
